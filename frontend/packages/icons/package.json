{
  "name": "@messenger/icons",
  "version": "0.0.1",
  "scripts": {
    "dev": "node ./scripts/dev.js",
    "build": "yarn build:icons && yarn build:components",
    "build:icons": "node ./scripts/build-icons.js",
    "build:components": "cross-env NODE_ENV=development babel src --out-dir lib --copy-files --ignore .test.js,stories.js --quiet",
    "clean": "rimraf lib"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/plugin-transform-modules-commonjs": "^7.2.0",
    "chokidar": "^2.0.3",
    "cross-env": "^5.1.4",
    "shelljs": "^0.8.1"
  }
}
